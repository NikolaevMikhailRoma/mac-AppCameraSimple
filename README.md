# Camera App

Simple app to take photos and record video from the MacBook's front camera.

## Launch

**`python` branch (current):**

```
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```
